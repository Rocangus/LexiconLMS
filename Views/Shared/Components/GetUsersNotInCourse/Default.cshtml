@model IEnumerable<SystemUserViewModel>
<ul class="list-unstyled" id="userlistul">
    @foreach (var user in Model)
    {
        <li>
            @user.Name <a class="adduserbutton" data-userid="@user.Id" data-courseid="@user.CourseId" href="">Add</a>
        </li>
    }
</ul>